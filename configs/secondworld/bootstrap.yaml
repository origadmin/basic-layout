sources:
  - type: env
    name: "env-vars"
    priority: 0

  - type: file
    name: "main-config"
    formats: [ yaml, yml ]
    file:
      path: "secondworld/conf.yaml"
    required: true
    watch: true
    priority: 100

paths:
  server: "server"
  logger: "logger"
  data: "data"
