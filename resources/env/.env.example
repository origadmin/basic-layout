# Example environment variables to override default configuration values.
# These variables are loaded by the 'env' source defined in bootstrap.yaml.
#
# To use, copy this file to '.env' and source it, or set variables directly in your shell.
# Example: source .env

# Override HTTP address for the currently running service
# Example: HTTP_ADDRESS=0.0.0.0:8080

# Override gRPC address for the currently running service
# Example: GRPC_ADDRESS=0.0.0.0:9090

# Override Consul discovery address
# Example: CONSUL_ADDRESS=127.0.0.1:8600