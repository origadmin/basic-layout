# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Greeter API
    description: The greeting service definition.
    version: 0.0.1
paths:
    /api/v1/greeter:
        get:
            tags:
                - Greeter
            description: ListGreeter lists Greeters
            operationId: Greeter_ListGreeter
            parameters:
                - name: current
                  in: query
                  description: Current page
                  schema:
                    type: integer
                    format: int32
                - name: pageSize
                  in: query
                  description: The number of lines per page
                  schema:
                    type: integer
                    format: int32
                - name: noPaging
                  in: query
                  description: Whether not paging
                  schema:
                    type: boolean
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListGreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - Greeter
            description: CreateGreeter creates a new Greeter
            operationId: Greeter_CreateGreeter
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GreeterData'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateGreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/greeter/{id}:
        get:
            tags:
                - Greeter
            description: GetGreeter gets a Greeter
            operationId: Greeter_GetGreeter
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetGreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        put:
            tags:
                - Greeter
            description: UpdateGreeter updates a Greeter
            operationId: Greeter_UpdateGreeter
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GreeterData'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateGreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        delete:
            tags:
                - Greeter
            description: DeleteGreeter deletes a Greeter
            operationId: Greeter_DeleteGreeter
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteGreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/greeter/{id}/hello:
        get:
            tags:
                - Greeter
            description: SayHello is a get method
            operationId: Greeter_SayHello
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: name
                  in: query
                  schema:
                    type: string
                - name: data.id
                  in: query
                  schema:
                    type: string
                - name: data.name
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - Greeter
            description: PostHello is a post method
            operationId: Greeter_PostHello
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: name
                  in: query
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GreeterData'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GreeterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateGreeterReply:
            type: object
            properties: {}
        DeleteGreeterReply:
            type: object
            properties: {}
        Error:
            type: object
            properties:
                id:
                    type: string
                code:
                    type: integer
                    format: int32
                detail:
                    type: string
        GetGreeterReply:
            type: object
            properties: {}
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        GreeterData:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
        GreeterReply:
            type: object
            properties:
                data:
                    $ref: '#/components/schemas/GreeterData'
            description: The response message containing the greetings
        ListGreeterReply:
            type: object
            properties:
                success:
                    type: boolean
                total:
                    type: integer
                    format: int32
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/GreeterData'
                error:
                    $ref: '#/components/schemas/Error'
                extra:
                    $ref: '#/components/schemas/GoogleProtobufAny'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateGreeterReply:
            type: object
            properties:
                success:
                    type: boolean
                data:
                    type: array
                    items:
                        $ref: '#/components/schemas/GreeterData'
tags:
    - name: Greeter
