sources:
  # File sources are loaded first to provide default values.
  - file:
      path: server.yaml
    type: file
  - file:
      path: clients.yaml # Added clients.yaml here
    type: file
  - file:
      path: databases.yaml
    type: file
  - file:
      path: logger.yaml
    type: file
  - file:
      path: middlewares.yaml
    type: file
  - file:
      path: trace.yaml
    type: file
  # Environment variables are loaded last to override file settings.
  - type: env
